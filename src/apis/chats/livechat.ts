

export const useAddLiveChat = (txt: string, callback: Function) => (async () => {

})().then(c => callback(c))

export const useUpdateLiveChat = (txt: string, callback: Function) => (async () => {
    
})().then(c => callback(c))

export const useRemoveLiveChat = (txt: string, callback: Function) => (async () => {
    
})().then(c => callback(c))